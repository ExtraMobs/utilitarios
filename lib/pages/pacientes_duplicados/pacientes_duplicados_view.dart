import 'package:flutter/material.dart';

class PacientesDuplicadosView extends StatelessWidget {
  const PacientesDuplicadosView({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: PacientesDuplicados(), backgroundColor: Colors.grey);
  }
}

class PacientesDuplicados extends StatelessWidget {
  const PacientesDuplicados({super.key});

  @override
  Widget build(BuildContext context) {
    return Column(children: []);
  }
}
